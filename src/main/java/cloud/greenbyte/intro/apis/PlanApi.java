/*
 * Greenbyte
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package cloud.greenbyte.intro.apis;

import cloud.greenbyte.intro.ApiHelper;
import cloud.greenbyte.intro.DateTimeHelper;
import cloud.greenbyte.intro.Server;
import cloud.greenbyte.intro.exceptions.ApiException;
import cloud.greenbyte.intro.exceptions.DeviceAccesses400ErrorException;
import cloud.greenbyte.intro.exceptions.DeviceAccesses429ErrorException;
import cloud.greenbyte.intro.exceptions.DowntimeEvents400ErrorException;
import cloud.greenbyte.intro.exceptions.DowntimeEvents429ErrorException;
import cloud.greenbyte.intro.exceptions.HseIncidents400ErrorException;
import cloud.greenbyte.intro.exceptions.HseIncidents429ErrorException;
import cloud.greenbyte.intro.exceptions.Organizations400ErrorException;
import cloud.greenbyte.intro.exceptions.Organizations429ErrorException;
import cloud.greenbyte.intro.exceptions.Personnel400ErrorException;
import cloud.greenbyte.intro.exceptions.Personnel429ErrorException;
import cloud.greenbyte.intro.exceptions.SiteAccesses400ErrorException;
import cloud.greenbyte.intro.exceptions.SiteAccesses429ErrorException;
import cloud.greenbyte.intro.exceptions.TaskCategories400ErrorException;
import cloud.greenbyte.intro.exceptions.TaskCategories429ErrorException;
import cloud.greenbyte.intro.exceptions.Tasks400ErrorException;
import cloud.greenbyte.intro.exceptions.Tasks429ErrorException;
import cloud.greenbyte.intro.exceptions.TasksComments400ErrorException;
import cloud.greenbyte.intro.exceptions.TasksComments429ErrorException;
import cloud.greenbyte.intro.exceptions.TasksFiles400ErrorException;
import cloud.greenbyte.intro.exceptions.TasksFiles429ErrorException;
import cloud.greenbyte.intro.exceptions.TasksFilesContent400ErrorException;
import cloud.greenbyte.intro.exceptions.TasksFilesContent429ErrorException;
import cloud.greenbyte.intro.exceptions.Worklog400ErrorException;
import cloud.greenbyte.intro.exceptions.Worklog429ErrorException;
import cloud.greenbyte.intro.http.request.HttpMethod;
import cloud.greenbyte.intro.http.response.ApiResponse;
import cloud.greenbyte.intro.models.DeviceAccess;
import cloud.greenbyte.intro.models.DeviceAccessesResponse;
import cloud.greenbyte.intro.models.DowntimeEvent;
import cloud.greenbyte.intro.models.DowntimeEventsResponse;
import cloud.greenbyte.intro.models.HseCategory;
import cloud.greenbyte.intro.models.HseIncidentsResponse;
import cloud.greenbyte.intro.models.HseIncidentsResponse1;
import cloud.greenbyte.intro.models.Organization;
import cloud.greenbyte.intro.models.Personnel;
import cloud.greenbyte.intro.models.PersonnelResponse;
import cloud.greenbyte.intro.models.SiteAccess;
import cloud.greenbyte.intro.models.SiteAccessesResponse;
import cloud.greenbyte.intro.models.State;
import cloud.greenbyte.intro.models.Task;
import cloud.greenbyte.intro.models.TaskCategory;
import cloud.greenbyte.intro.models.TaskComment;
import cloud.greenbyte.intro.models.TaskState;
import cloud.greenbyte.intro.models.TasksFilesResponse;
import cloud.greenbyte.intro.models.TasksResponse;
import cloud.greenbyte.intro.models.WorklogResponse;
import cloud.greenbyte.intro.models.WorklogResponse1;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.io.InputStream;
import java.time.LocalDateTime;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class PlanApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public PlanApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Gets a list of tasks. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get tasks for.
     * @param  siteIds  Optional parameter: What sites to get tasks for.
     * @param  categoryIds  Optional parameter: What task categories to include.
     * @param  state  Optional parameter: What state of tasks to get: resolved and unresolved. If
     *         not set, both resolved and unresolved tasks are included.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Task` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of Task wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Task>> listTasks(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<Integer> categoryIds,
            final TaskState state,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListTasksRequest(timestampStart, timestampEnd, deviceIds, siteIds,
                categoryIds, state, fields, pageSize, page, useUtc).execute();
    }

    /**
     * Gets a list of tasks. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get tasks for.
     * @param  siteIds  Optional parameter: What sites to get tasks for.
     * @param  categoryIds  Optional parameter: What task categories to include.
     * @param  state  Optional parameter: What state of tasks to get: resolved and unresolved. If
     *         not set, both resolved and unresolved tasks are included.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Task` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of Task wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Task>>> listTasksAsync(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<Integer> categoryIds,
            final TaskState state,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListTasksRequest(timestampStart, timestampEnd, deviceIds, siteIds, categoryIds,
            state, fields, pageSize, page, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listTasks.
     */
    private ApiCall<ApiResponse<List<Task>>, ApiException> prepareListTasksRequest(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<Integer> categoryIds,
            final TaskState state,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<Task>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/tasks")
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds).isRequired(false))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("categoryIds")
                                .value(categoryIds).isRequired(false))
                        .queryParam(param -> param.key("state")
                                .value((state != null) ? state.value() : null).isRequired(false))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Task[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Tasks400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Tasks429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get a single task by ID. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  taskId  Required parameter: The id of the task to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the TasksResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TasksResponse> getTask(
            final int taskId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetTaskRequest(taskId, useUtc).execute();
    }

    /**
     * Get a single task by ID. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  taskId  Required parameter: The id of the task to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the TasksResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TasksResponse>> getTaskAsync(
            final int taskId,
            final Boolean useUtc) {
        try {
            return prepareGetTaskRequest(taskId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getTask.
     */
    private ApiCall<ApiResponse<TasksResponse>, ApiException> prepareGetTaskRequest(
            final int taskId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<TasksResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/tasks/{taskId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("taskId").value(taskId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TasksResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Tasks400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Tasks429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of task categories. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @return    Returns the List of TaskCategory wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<TaskCategory>> listTaskCategories() throws ApiException, IOException {
        return prepareListTaskCategoriesRequest().execute();
    }

    /**
     * Gets a list of task categories. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @return    Returns the List of TaskCategory wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<TaskCategory>>> listTaskCategoriesAsync() {
        try {
            return prepareListTaskCategoriesRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listTaskCategories.
     */
    private ApiCall<ApiResponse<List<TaskCategory>>, ApiException> prepareListTaskCategoriesRequest() {
        return new ApiCall.Builder<ApiResponse<List<TaskCategory>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/task-categories")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        TaskCategory[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new TaskCategories400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new TaskCategories429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of comments belonging to one or more tasks with given taskIds. _🔐 This endpoint
     * requires the **Plan** endpoint permission._.
     * @param  taskIds  Required parameter: An array of taskIds.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `TaskComment` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of TaskComment wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<TaskComment>> listCommentsForMultipleTasks(
            final List<Integer> taskIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListCommentsForMultipleTasksRequest(taskIds, fields, pageSize, page,
                useUtc).execute();
    }

    /**
     * Gets a list of comments belonging to one or more tasks with given taskIds. _🔐 This endpoint
     * requires the **Plan** endpoint permission._.
     * @param  taskIds  Required parameter: An array of taskIds.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `TaskComment` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of TaskComment wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<TaskComment>>> listCommentsForMultipleTasksAsync(
            final List<Integer> taskIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListCommentsForMultipleTasksRequest(taskIds, fields, pageSize, page,
            useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listCommentsForMultipleTasks.
     */
    private ApiCall<ApiResponse<List<TaskComment>>, ApiException> prepareListCommentsForMultipleTasksRequest(
            final List<Integer> taskIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<TaskComment>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/tasks-comments")
                        .queryParam(param -> param.key("taskIds")
                                .value(taskIds))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        TaskComment[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new TasksComments400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new TasksComments429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of files belonging to a task. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  taskId  Required parameter: The id of the task.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `TaskFile` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of TasksFilesResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<TasksFilesResponse>> listTaskFiles(
            final int taskId,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListTaskFilesRequest(taskId, fields, pageSize, page, useUtc).execute();
    }

    /**
     * Gets a list of files belonging to a task. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  taskId  Required parameter: The id of the task.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `TaskFile` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of TasksFilesResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<TasksFilesResponse>>> listTaskFilesAsync(
            final int taskId,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListTaskFilesRequest(taskId, fields, pageSize, page, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listTaskFiles.
     */
    private ApiCall<ApiResponse<List<TasksFilesResponse>>, ApiException> prepareListTaskFilesRequest(
            final int taskId,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<TasksFilesResponse>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/tasks/{taskId}/files")
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("taskId").value(taskId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        TasksFilesResponse[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new TasksFiles400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new TasksFiles429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Downloads a file belonging to a task. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  taskId  Required parameter: The id of the task.
     * @param  fileId  Required parameter: The id of the file.
     * @return    Returns the InputStream wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<InputStream> downloadTaskFile(
            final int taskId,
            final int fileId) throws ApiException, IOException {
        return prepareDownloadTaskFileRequest(taskId, fileId).execute();
    }

    /**
     * Downloads a file belonging to a task. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  taskId  Required parameter: The id of the task.
     * @param  fileId  Required parameter: The id of the file.
     * @return    Returns the InputStream wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<InputStream>> downloadTaskFileAsync(
            final int taskId,
            final int fileId) {
        try {
            return prepareDownloadTaskFileRequest(taskId, fileId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for downloadTaskFile.
     */
    private ApiCall<ApiResponse<InputStream>, ApiException> prepareDownloadTaskFileRequest(
            final int taskId,
            final int fileId) {
        return new ApiCall.Builder<ApiResponse<InputStream>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/tasks/{taskId}/files/{fileId}/content")
                        .templateParam(param -> param.key("taskId").value(taskId).isRequired(false)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("fileId").value(fileId).isRequired(false)
                                .shouldEncode(true))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new TasksFilesContent400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new TasksFilesContent429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .endpointConfiguration(param -> param
                                .hasBinaryResponse(true))
                .build();
    }

    /**
     * Gets a list of site accesses. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get site accesses for.
     * @param  siteIds  Optional parameter: What sites to get site accesses for.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `SiteAccess` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of SiteAccess wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<SiteAccess>> listSiteAccesses(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListSiteAccessesRequest(timestampStart, timestampEnd, deviceIds, siteIds,
                fields, pageSize, page, useUtc).execute();
    }

    /**
     * Gets a list of site accesses. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get site accesses for.
     * @param  siteIds  Optional parameter: What sites to get site accesses for.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `SiteAccess` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of SiteAccess wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<SiteAccess>>> listSiteAccessesAsync(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListSiteAccessesRequest(timestampStart, timestampEnd, deviceIds, siteIds, fields,
            pageSize, page, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listSiteAccesses.
     */
    private ApiCall<ApiResponse<List<SiteAccess>>, ApiException> prepareListSiteAccessesRequest(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<SiteAccess>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/site-accesses")
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds).isRequired(false))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        SiteAccess[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new SiteAccesses400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new SiteAccesses429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a specific site access. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  siteAccessId  Required parameter: The id of the site access.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the SiteAccessesResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<SiteAccessesResponse> getSiteAccess(
            final int siteAccessId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetSiteAccessRequest(siteAccessId, useUtc).execute();
    }

    /**
     * Gets a specific site access. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  siteAccessId  Required parameter: The id of the site access.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the SiteAccessesResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<SiteAccessesResponse>> getSiteAccessAsync(
            final int siteAccessId,
            final Boolean useUtc) {
        try {
            return prepareGetSiteAccessRequest(siteAccessId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getSiteAccess.
     */
    private ApiCall<ApiResponse<SiteAccessesResponse>, ApiException> prepareGetSiteAccessRequest(
            final int siteAccessId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<SiteAccessesResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/site-accesses/{siteAccessId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("siteAccessId").value(siteAccessId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, SiteAccessesResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new SiteAccesses400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new SiteAccesses429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of device accesses belonging to site accesses with specified SiteAccessIds. _🔐
     * This endpoint requires the **Plan** endpoint permission._.
     * @param  siteAccessIds  Required parameter: An array of siteAccessIds.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `DeviceAccess` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of DeviceAccess wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DeviceAccess>> listDeviceAccessesForMultipleSiteAccesses(
            final List<Integer> siteAccessIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListDeviceAccessesForMultipleSiteAccessesRequest(siteAccessIds, fields,
                pageSize, page, useUtc).execute();
    }

    /**
     * Gets a list of device accesses belonging to site accesses with specified SiteAccessIds. _🔐
     * This endpoint requires the **Plan** endpoint permission._.
     * @param  siteAccessIds  Required parameter: An array of siteAccessIds.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `DeviceAccess` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of DeviceAccess wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DeviceAccess>>> listDeviceAccessesForMultipleSiteAccessesAsync(
            final List<Integer> siteAccessIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListDeviceAccessesForMultipleSiteAccessesRequest(siteAccessIds, fields, pageSize,
            page, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listDeviceAccessesForMultipleSiteAccesses.
     */
    private ApiCall<ApiResponse<List<DeviceAccess>>, ApiException> prepareListDeviceAccessesForMultipleSiteAccessesRequest(
            final List<Integer> siteAccessIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<DeviceAccess>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/device-accesses")
                        .queryParam(param -> param.key("siteAccessIds")
                                .value(siteAccessIds))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DeviceAccess[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new DeviceAccesses400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new DeviceAccesses429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get a single device access by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  deviceAccessId  Required parameter: The id of the device access to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the DeviceAccessesResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DeviceAccessesResponse> getDeviceAccess(
            final int deviceAccessId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetDeviceAccessRequest(deviceAccessId, useUtc).execute();
    }

    /**
     * Get a single device access by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  deviceAccessId  Required parameter: The id of the device access to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the DeviceAccessesResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DeviceAccessesResponse>> getDeviceAccessAsync(
            final int deviceAccessId,
            final Boolean useUtc) {
        try {
            return prepareGetDeviceAccessRequest(deviceAccessId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDeviceAccess.
     */
    private ApiCall<ApiResponse<DeviceAccessesResponse>, ApiException> prepareGetDeviceAccessRequest(
            final int deviceAccessId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<DeviceAccessesResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/device-accesses/{deviceAccessId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("deviceAccessId").value(deviceAccessId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DeviceAccessesResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new DeviceAccesses400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new DeviceAccesses429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of downtime events. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get downtime events for.
     * @param  siteIds  Optional parameter: What sites to get downtime events for.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `DowntimeEvent` schema (See Response Type). By default all
     *         fields are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of DowntimeEvent wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DowntimeEvent>> listDowntimeEvents(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListDowntimeEventsRequest(timestampStart, timestampEnd, deviceIds, siteIds,
                fields, pageSize, page, useUtc).execute();
    }

    /**
     * Gets a list of downtime events. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  deviceIds  Optional parameter: What devices to get downtime events for.
     * @param  siteIds  Optional parameter: What sites to get downtime events for.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `DowntimeEvent` schema (See Response Type). By default all
     *         fields are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of DowntimeEvent wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DowntimeEvent>>> listDowntimeEventsAsync(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareListDowntimeEventsRequest(timestampStart, timestampEnd, deviceIds, siteIds,
            fields, pageSize, page, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listDowntimeEvents.
     */
    private ApiCall<ApiResponse<List<DowntimeEvent>>, ApiException> prepareListDowntimeEventsRequest(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> deviceIds,
            final List<Integer> siteIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<DowntimeEvent>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/downtime-events")
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds).isRequired(false))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DowntimeEvent[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new DowntimeEvents400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new DowntimeEvents429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a single downtime event by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  downtimeEventId  Required parameter: The id of the downtime event to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the DowntimeEventsResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DowntimeEventsResponse> getDowntimeEvent(
            final int downtimeEventId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetDowntimeEventRequest(downtimeEventId, useUtc).execute();
    }

    /**
     * Gets a single downtime event by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._.
     * @param  downtimeEventId  Required parameter: The id of the downtime event to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the DowntimeEventsResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DowntimeEventsResponse>> getDowntimeEventAsync(
            final int downtimeEventId,
            final Boolean useUtc) {
        try {
            return prepareGetDowntimeEventRequest(downtimeEventId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDowntimeEvent.
     */
    private ApiCall<ApiResponse<DowntimeEventsResponse>, ApiException> prepareGetDowntimeEventRequest(
            final int downtimeEventId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<DowntimeEventsResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/downtime-events/{downtimeEventId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("downtimeEventId").value(downtimeEventId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DowntimeEventsResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new DowntimeEvents400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new DowntimeEvents429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of personnel. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Personnel` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @return    Returns the List of Personnel wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Personnel>> listPersonnel(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) throws ApiException, IOException {
        return prepareListPersonnelRequest(fields, pageSize, page).execute();
    }

    /**
     * Gets a list of personnel. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Personnel` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @return    Returns the List of Personnel wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Personnel>>> listPersonnelAsync(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) {
        try {
            return prepareListPersonnelRequest(fields, pageSize, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listPersonnel.
     */
    private ApiCall<ApiResponse<List<Personnel>>, ApiException> prepareListPersonnelRequest(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<List<Personnel>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/personnel")
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Personnel[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Personnel400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Personnel429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a single personnel by ID. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  personnelId  Required parameter: The id of the personnel to get.
     * @return    Returns the PersonnelResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PersonnelResponse> getPersonnel(
            final int personnelId) throws ApiException, IOException {
        return prepareGetPersonnelRequest(personnelId).execute();
    }

    /**
     * Gets a single personnel by ID. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @param  personnelId  Required parameter: The id of the personnel to get.
     * @return    Returns the PersonnelResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PersonnelResponse>> getPersonnelAsync(
            final int personnelId) {
        try {
            return prepareGetPersonnelRequest(personnelId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getPersonnel.
     */
    private ApiCall<ApiResponse<PersonnelResponse>, ApiException> prepareGetPersonnelRequest(
            final int personnelId) {
        return new ApiCall.Builder<ApiResponse<PersonnelResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/personnel/{personnelId}")
                        .templateParam(param -> param.key("personnelId").value(personnelId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PersonnelResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Personnel400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Personnel429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of organizations. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @return    Returns the List of Organization wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Organization>> listOrganizations() throws ApiException, IOException {
        return prepareListOrganizationsRequest().execute();
    }

    /**
     * Gets a list of organizations. _🔐 This endpoint requires the **Plan** endpoint permission._.
     * @return    Returns the List of Organization wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Organization>>> listOrganizationsAsync() {
        try {
            return prepareListOrganizationsRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listOrganizations.
     */
    private ApiCall<ApiResponse<List<Organization>>, ApiException> prepareListOrganizationsRequest() {
        return new ApiCall.Builder<ApiResponse<List<Organization>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/organizations")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Organization[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Organizations400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Organizations429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of HSE incidents. _🔐 This endpoint requires the **Plan** endpoint permission._
     * _This is a beta feature. Some details might change before it is released as a stable
     * version._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  siteIds  Optional parameter: Which sites to get HSE incidents for.
     * @param  state  Optional parameter: Retrieve HSE incidents with state: resolved or unresolved.
     *         If not set, both resolved and unresolved HSE incidents are included.
     * @param  category  Optional parameter: Retrieve HSE incidents with a specific category. If not
     *         set, HSE incidents of all categories are included.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of HseIncidentsResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<HseIncidentsResponse>> listHseIncidents(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final State state,
            final HseCategory category,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListHseIncidentsRequest(timestampStart, timestampEnd, siteIds, state,
                category, useUtc).execute();
    }

    /**
     * Gets a list of HSE incidents. _🔐 This endpoint requires the **Plan** endpoint permission._
     * _This is a beta feature. Some details might change before it is released as a stable
     * version._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  siteIds  Optional parameter: Which sites to get HSE incidents for.
     * @param  state  Optional parameter: Retrieve HSE incidents with state: resolved or unresolved.
     *         If not set, both resolved and unresolved HSE incidents are included.
     * @param  category  Optional parameter: Retrieve HSE incidents with a specific category. If not
     *         set, HSE incidents of all categories are included.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of HseIncidentsResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<HseIncidentsResponse>>> listHseIncidentsAsync(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final State state,
            final HseCategory category,
            final Boolean useUtc) {
        try {
            return prepareListHseIncidentsRequest(timestampStart, timestampEnd, siteIds, state, category,
            useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listHseIncidents.
     */
    private ApiCall<ApiResponse<List<HseIncidentsResponse>>, ApiException> prepareListHseIncidentsRequest(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final State state,
            final HseCategory category,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<HseIncidentsResponse>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/hse-incidents")
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("state")
                                .value((state != null) ? state.value() : null).isRequired(false))
                        .queryParam(param -> param.key("category")
                                .value((category != null) ? category.value() : null).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        HseIncidentsResponse[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new HseIncidents400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new HseIncidents429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get a single HSE incident by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._ _This is a beta feature. Some details might change before it is released as a
     * stable version._.
     * @param  hseIncidentId  Required parameter: The id of the HSE incident to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the HseIncidentsResponse1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<HseIncidentsResponse1> getHseIncident(
            final int hseIncidentId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetHseIncidentRequest(hseIncidentId, useUtc).execute();
    }

    /**
     * Get a single HSE incident by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._ _This is a beta feature. Some details might change before it is released as a
     * stable version._.
     * @param  hseIncidentId  Required parameter: The id of the HSE incident to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the HseIncidentsResponse1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<HseIncidentsResponse1>> getHseIncidentAsync(
            final int hseIncidentId,
            final Boolean useUtc) {
        try {
            return prepareGetHseIncidentRequest(hseIncidentId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getHseIncident.
     */
    private ApiCall<ApiResponse<HseIncidentsResponse1>, ApiException> prepareGetHseIncidentRequest(
            final int hseIncidentId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<HseIncidentsResponse1>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/hse-incidents/{hseIncidentId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("hseIncidentId").value(hseIncidentId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, HseIncidentsResponse1.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new HseIncidents400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new HseIncidents429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of worklog items. _🔐 This endpoint requires the **Plan** endpoint permission._
     * _This is a beta feature. Some details might change before it is released as a stable
     * version._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  siteIds  Optional parameter: What sites to get worklog items for.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of WorklogResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<WorklogResponse>> listWorklogItems(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareListWorklogItemsRequest(timestampStart, timestampEnd, siteIds,
                useUtc).execute();
    }

    /**
     * Gets a list of worklog items. _🔐 This endpoint requires the **Plan** endpoint permission._
     * _This is a beta feature. Some details might change before it is released as a stable
     * version._.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  siteIds  Optional parameter: What sites to get worklog items for.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of WorklogResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<WorklogResponse>>> listWorklogItemsAsync(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final Boolean useUtc) {
        try {
            return prepareListWorklogItemsRequest(timestampStart, timestampEnd, siteIds,
            useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for listWorklogItems.
     */
    private ApiCall<ApiResponse<List<WorklogResponse>>, ApiException> prepareListWorklogItemsRequest(
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final List<Integer> siteIds,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<WorklogResponse>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/worklog")
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        WorklogResponse[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Worklog400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Worklog429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Get a single worklog item by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._ _This is a beta feature. Some details might change before it is released as a
     * stable version._.
     * @param  worklogItemId  Required parameter: The id of the worklog item to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the WorklogResponse1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<WorklogResponse1> getWorklogItem(
            final int worklogItemId,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetWorklogItemRequest(worklogItemId, useUtc).execute();
    }

    /**
     * Get a single worklog item by ID. _🔐 This endpoint requires the **Plan** endpoint
     * permission._ _This is a beta feature. Some details might change before it is released as a
     * stable version._.
     * @param  worklogItemId  Required parameter: The id of the worklog item to get.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the WorklogResponse1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<WorklogResponse1>> getWorklogItemAsync(
            final int worklogItemId,
            final Boolean useUtc) {
        try {
            return prepareGetWorklogItemRequest(worklogItemId, useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getWorklogItem.
     */
    private ApiCall<ApiResponse<WorklogResponse1>, ApiException> prepareGetWorklogItemRequest(
            final int worklogItemId,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<WorklogResponse1>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/worklog/{worklogItemId}")
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .templateParam(param -> param.key("worklogItemId").value(worklogItemId).isRequired(false)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, WorklogResponse1.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Worklog400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("404",
                                 ErrorCase.setReason("The requested resource could not be found.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Worklog429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}
/*
 * Greenbyte
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package cloud.greenbyte.intro.apis;

import cloud.greenbyte.intro.ApiHelper;
import cloud.greenbyte.intro.DateTimeHelper;
import cloud.greenbyte.intro.Server;
import cloud.greenbyte.intro.exceptions.ApiException;
import cloud.greenbyte.intro.exceptions.Devices400ErrorException;
import cloud.greenbyte.intro.exceptions.Devices429ErrorException;
import cloud.greenbyte.intro.exceptions.DevicesPublishedAfterDate400ErrorException;
import cloud.greenbyte.intro.exceptions.DevicesPublishedAfterDate429ErrorException;
import cloud.greenbyte.intro.exceptions.Powercurves400ErrorException;
import cloud.greenbyte.intro.exceptions.Powercurves429ErrorException;
import cloud.greenbyte.intro.exceptions.Sites400ErrorException;
import cloud.greenbyte.intro.exceptions.Sites429ErrorException;
import cloud.greenbyte.intro.http.request.HttpMethod;
import cloud.greenbyte.intro.http.response.ApiResponse;
import cloud.greenbyte.intro.models.Device;
import cloud.greenbyte.intro.models.DevicesPublishedAfterDateResponse;
import cloud.greenbyte.intro.models.PowerCurve;
import cloud.greenbyte.intro.models.SiteWithData;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class AssetsApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public AssetsApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Gets a list of devices that the API key has permissions for. _🔐 This endpoint requires the
     * **Assets** endpoint permission._ _This request can also be made using the POST method, with a
     * request to `devices.json` and a JSON request body instead of query parameters._.
     * @param  deviceTypeIds  Optional parameter: Only include devices of these types. Examples: * 1
     *         - Wind turbine * 2 - Production meter * 3 - Met mast * 4 - Inverter * 5 - Substation
     *         * 10 - Device group * 11 - Grid meter * 12 - Combiner box * 23 - String * 27 -
     *         Virtual Meteo Sensor
     * @param  siteIds  Optional parameter: Only include devices at these sites.
     * @param  parentIds  Optional parameter: Only include devices with these parent devices.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Device` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of Device wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Device>> getDevices(
            final List<Integer> deviceTypeIds,
            final List<Integer> siteIds,
            final List<Integer> parentIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) throws ApiException, IOException {
        return prepareGetDevicesRequest(deviceTypeIds, siteIds, parentIds, fields, pageSize, page,
                useUtc).execute();
    }

    /**
     * Gets a list of devices that the API key has permissions for. _🔐 This endpoint requires the
     * **Assets** endpoint permission._ _This request can also be made using the POST method, with a
     * request to `devices.json` and a JSON request body instead of query parameters._.
     * @param  deviceTypeIds  Optional parameter: Only include devices of these types. Examples: * 1
     *         - Wind turbine * 2 - Production meter * 3 - Met mast * 4 - Inverter * 5 - Substation
     *         * 10 - Device group * 11 - Grid meter * 12 - Combiner box * 23 - String * 27 -
     *         Virtual Meteo Sensor
     * @param  siteIds  Optional parameter: Only include devices at these sites.
     * @param  parentIds  Optional parameter: Only include devices with these parent devices.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `Device` schema (See Response Type). By default all fields are
     *         included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC.
     * @return    Returns the List of Device wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Device>>> getDevicesAsync(
            final List<Integer> deviceTypeIds,
            final List<Integer> siteIds,
            final List<Integer> parentIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        try {
            return prepareGetDevicesRequest(deviceTypeIds, siteIds, parentIds, fields, pageSize, page,
            useUtc).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDevices.
     */
    private ApiCall<ApiResponse<List<Device>>, ApiException> prepareGetDevicesRequest(
            final List<Integer> deviceTypeIds,
            final List<Integer> siteIds,
            final List<Integer> parentIds,
            final List<String> fields,
            final Integer pageSize,
            final Integer page,
            final Boolean useUtc) {
        return new ApiCall.Builder<ApiResponse<List<Device>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/devices")
                        .queryParam(param -> param.key("deviceTypeIds")
                                .value(deviceTypeIds).isRequired(false))
                        .queryParam(param -> param.key("siteIds")
                                .value(siteIds).isRequired(false))
                        .queryParam(param -> param.key("parentIds")
                                .value(parentIds).isRequired(false))
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Device[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Devices400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Devices429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets the number of devices published on a site after a certain date as well as the IDs of the
     * authorized devices. _🔐 This endpoint requires the **Assets** endpoint permission._.
     * @param  siteId  Required parameter:
     * @param  date  Required parameter: Date after which published devices will be fetched.
     * @return    Returns the DevicesPublishedAfterDateResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DevicesPublishedAfterDateResponse> getDevicesPublishedAfterDate(
            final int siteId,
            final LocalDateTime date) throws ApiException, IOException {
        return prepareGetDevicesPublishedAfterDateRequest(siteId, date).execute();
    }

    /**
     * Gets the number of devices published on a site after a certain date as well as the IDs of the
     * authorized devices. _🔐 This endpoint requires the **Assets** endpoint permission._.
     * @param  siteId  Required parameter:
     * @param  date  Required parameter: Date after which published devices will be fetched.
     * @return    Returns the DevicesPublishedAfterDateResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DevicesPublishedAfterDateResponse>> getDevicesPublishedAfterDateAsync(
            final int siteId,
            final LocalDateTime date) {
        try {
            return prepareGetDevicesPublishedAfterDateRequest(siteId, date).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDevicesPublishedAfterDate.
     */
    private ApiCall<ApiResponse<DevicesPublishedAfterDateResponse>, ApiException> prepareGetDevicesPublishedAfterDateRequest(
            final int siteId,
            final LocalDateTime date) {
        return new ApiCall.Builder<ApiResponse<DevicesPublishedAfterDateResponse>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/devices-published-after-date")
                        .queryParam(param -> param.key("siteId")
                                .value(siteId).isRequired(false))
                        .queryParam(param -> param.key("date")
                                .value(DateTimeHelper.toRfc8601DateTime(date)))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DevicesPublishedAfterDateResponse.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new DevicesPublishedAfterDate400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new DevicesPublishedAfterDate429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets a list of sites that the API key has permissions for. _🔐 This endpoint requires the
     * **Assets** endpoint permission._ _This request can also be made using the POST method, with a
     * request to `sites.json` and a JSON request body instead of query parameters._.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `SiteWithData` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @return    Returns the List of SiteWithData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<SiteWithData>> getSites(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) throws ApiException, IOException {
        return prepareGetSitesRequest(fields, pageSize, page).execute();
    }

    /**
     * Gets a list of sites that the API key has permissions for. _🔐 This endpoint requires the
     * **Assets** endpoint permission._ _This request can also be made using the POST method, with a
     * request to `sites.json` and a JSON request body instead of query parameters._.
     * @param  fields  Optional parameter: Which fields to include in the response. Valid fields are
     *         those defined in the `SiteWithData` schema (See Response Type). By default all fields
     *         are included.
     * @param  pageSize  Optional parameter: The number of items to return per page.
     * @param  page  Optional parameter: Which page to return when the number of items exceed the
     *         page size.
     * @return    Returns the List of SiteWithData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<SiteWithData>>> getSitesAsync(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) {
        try {
            return prepareGetSitesRequest(fields, pageSize, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getSites.
     */
    private ApiCall<ApiResponse<List<SiteWithData>>, ApiException> prepareGetSitesRequest(
            final List<String> fields,
            final Integer pageSize,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<List<SiteWithData>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/sites")
                        .queryParam(param -> param.key("fields")
                                .value(fields).isRequired(false))
                        .queryParam(param -> param.key("pageSize")
                                .value((pageSize != null) ? pageSize : 50).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value((page != null) ? page : 1).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        SiteWithData[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Sites400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Sites429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets the default or learned power curves for wind turbines. Other device types are not
     * supported. _🔐 This endpoint requires the **PowerCurves** endpoint permission._ _This request
     * can also be made using the POST method, with a request to `powercurves.json` and a JSON
     * request body instead of query parameters._.
     * @param  deviceIds  Required parameter: What devices to get power curves for. Only wind
     *         turbines are supported.
     * @param  timestamp  Optional parameter: The date for which to get power curves. The default is
     *         the current date.
     * @param  learned  Optional parameter: Whether to get learned power curves instead of default
     *         power curves.
     * @return    Returns the List of PowerCurve wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<PowerCurve>> getPowerCurves(
            final List<Integer> deviceIds,
            final LocalDate timestamp,
            final Boolean learned) throws ApiException, IOException {
        return prepareGetPowerCurvesRequest(deviceIds, timestamp, learned).execute();
    }

    /**
     * Gets the default or learned power curves for wind turbines. Other device types are not
     * supported. _🔐 This endpoint requires the **PowerCurves** endpoint permission._ _This request
     * can also be made using the POST method, with a request to `powercurves.json` and a JSON
     * request body instead of query parameters._.
     * @param  deviceIds  Required parameter: What devices to get power curves for. Only wind
     *         turbines are supported.
     * @param  timestamp  Optional parameter: The date for which to get power curves. The default is
     *         the current date.
     * @param  learned  Optional parameter: Whether to get learned power curves instead of default
     *         power curves.
     * @return    Returns the List of PowerCurve wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<PowerCurve>>> getPowerCurvesAsync(
            final List<Integer> deviceIds,
            final LocalDate timestamp,
            final Boolean learned) {
        try {
            return prepareGetPowerCurvesRequest(deviceIds, timestamp, learned).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getPowerCurves.
     */
    private ApiCall<ApiResponse<List<PowerCurve>>, ApiException> prepareGetPowerCurvesRequest(
            final List<Integer> deviceIds,
            final LocalDate timestamp,
            final Boolean learned) {
        return new ApiCall.Builder<ApiResponse<List<PowerCurve>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/powercurves")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds))
                        .queryParam(param -> param.key("timestamp")
                                .value(DateTimeHelper.toSimpleDate(timestamp)).isRequired(false))
                        .queryParam(param -> param.key("learned")
                                .value((learned != null) ? learned : false).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        PowerCurve[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Powercurves400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Powercurves429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}
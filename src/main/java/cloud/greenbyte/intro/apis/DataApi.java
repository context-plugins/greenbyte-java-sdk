/*
 * GreenbyteApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package cloud.greenbyte.intro.apis;

import cloud.greenbyte.intro.ApiHelper;
import cloud.greenbyte.intro.DateTimeHelper;
import cloud.greenbyte.intro.Server;
import cloud.greenbyte.intro.exceptions.ApiException;
import cloud.greenbyte.intro.exceptions.Data400ErrorException;
import cloud.greenbyte.intro.exceptions.Data429ErrorException;
import cloud.greenbyte.intro.exceptions.Datapercategory400ErrorException;
import cloud.greenbyte.intro.exceptions.Datapercategory429ErrorException;
import cloud.greenbyte.intro.exceptions.Datasignals400ErrorException;
import cloud.greenbyte.intro.exceptions.Datasignals429ErrorException;
import cloud.greenbyte.intro.exceptions.Highresdata400ErrorException;
import cloud.greenbyte.intro.exceptions.Highresdata429ErrorException;
import cloud.greenbyte.intro.exceptions.Realtimedata400ErrorException;
import cloud.greenbyte.intro.exceptions.Realtimedata429ErrorException;
import cloud.greenbyte.intro.http.request.HttpMethod;
import cloud.greenbyte.intro.http.response.ApiResponse;
import cloud.greenbyte.intro.models.AggregateMode;
import cloud.greenbyte.intro.models.CalculationMode;
import cloud.greenbyte.intro.models.CalculationModeRealTime;
import cloud.greenbyte.intro.models.ContractType1;
import cloud.greenbyte.intro.models.DataItem;
import cloud.greenbyte.intro.models.DataRealTimeItem;
import cloud.greenbyte.intro.models.DataSignalItem;
import cloud.greenbyte.intro.models.DatapercategoryResponse1;
import cloud.greenbyte.intro.models.HighresdataResponse;
import cloud.greenbyte.intro.models.Resolution;
import cloud.greenbyte.intro.models.StatusCategory;
import io.apimatic.core.ApiCall;
import io.apimatic.core.ErrorCase;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.time.LocalDateTime;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class DataApi extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public DataApi(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * Gets authorized data signals for one or more devices. _🔐 This endpoint requires the **Data**
     * endpoint permission._ _This request can also be made using the POST method, with a request to
     * `datasignals.json` and a JSON request body instead of query parameters._.
     * @param  deviceIds  Optional parameter: What devices to get data signals for.
     * @return    Returns the List of DataSignalItem wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DataSignalItem>> getDataSignals(
            final List<Integer> deviceIds) throws ApiException, IOException {
        return prepareGetDataSignalsRequest(deviceIds).execute();
    }

    /**
     * Gets authorized data signals for one or more devices. _🔐 This endpoint requires the **Data**
     * endpoint permission._ _This request can also be made using the POST method, with a request to
     * `datasignals.json` and a JSON request body instead of query parameters._.
     * @param  deviceIds  Optional parameter: What devices to get data signals for.
     * @return    Returns the List of DataSignalItem wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DataSignalItem>>> getDataSignalsAsync(
            final List<Integer> deviceIds) {
        try {
            return prepareGetDataSignalsRequest(deviceIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDataSignals.
     */
    private ApiCall<ApiResponse<List<DataSignalItem>>, ApiException> prepareGetDataSignalsRequest(
            final List<Integer> deviceIds) {
        return new ApiCall.Builder<ApiResponse<List<DataSignalItem>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/datasignals")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DataSignalItem[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Datasignals400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Datasignals429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets data for multiple devices and data signals in the given resolution. The timestamps are
     * in the time zone configured in the Greenbyte Platform. Use the useUtc flag to get timestamps
     * in UTC for all resolutions other than daily, weekly, monthly and yearly. _🔐 This endpoint
     * requires the **Data** endpoint permission._ _This request can also be made using the POST
     * method, with a request to `data.json` and a JSON request body instead of query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalIds  Required parameter: Which data signals to get data for.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC. UTC timestamps are
     *         available for all resolutions other than daily, weekly, monthly and yearly.
     * @param  resolution  Optional parameter: The desired data resolution.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  calculation  Optional parameter: The calculation used when aggregating data, both
     *         over time and across devices. The default is the data signal default.
     * @return    Returns the List of DataItem wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DataItem>> getData(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final Boolean useUtc,
            final Resolution resolution,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationMode calculation) throws ApiException, IOException {
        return prepareGetDataRequest(deviceIds, dataSignalIds, timestampStart, timestampEnd, useUtc,
                resolution, aggregate, aggregateLevel, calculation).execute();
    }

    /**
     * Gets data for multiple devices and data signals in the given resolution. The timestamps are
     * in the time zone configured in the Greenbyte Platform. Use the useUtc flag to get timestamps
     * in UTC for all resolutions other than daily, weekly, monthly and yearly. _🔐 This endpoint
     * requires the **Data** endpoint permission._ _This request can also be made using the POST
     * method, with a request to `data.json` and a JSON request body instead of query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalIds  Required parameter: Which data signals to get data for.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  useUtc  Optional parameter: Set to true to get timestamps in UTC. UTC timestamps are
     *         available for all resolutions other than daily, weekly, monthly and yearly.
     * @param  resolution  Optional parameter: The desired data resolution.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  calculation  Optional parameter: The calculation used when aggregating data, both
     *         over time and across devices. The default is the data signal default.
     * @return    Returns the List of DataItem wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DataItem>>> getDataAsync(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final Boolean useUtc,
            final Resolution resolution,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationMode calculation) {
        try {
            return prepareGetDataRequest(deviceIds, dataSignalIds, timestampStart, timestampEnd, useUtc,
            resolution, aggregate, aggregateLevel, calculation).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getData.
     */
    private ApiCall<ApiResponse<List<DataItem>>, ApiException> prepareGetDataRequest(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final Boolean useUtc,
            final Resolution resolution,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationMode calculation) {
        return new ApiCall.Builder<ApiResponse<List<DataItem>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/data")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds))
                        .queryParam(param -> param.key("dataSignalIds")
                                .value(dataSignalIds))
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("useUtc")
                                .value((useUtc != null) ? useUtc : false).isRequired(false))
                        .queryParam(param -> param.key("resolution")
                                .value((resolution != null) ? resolution.value() : null).isRequired(false))
                        .queryParam(param -> param.key("aggregate")
                                .value((aggregate != null) ? aggregate.value() : null).isRequired(false))
                        .queryParam(param -> param.key("aggregateLevel")
                                .value((aggregateLevel != null) ? aggregateLevel : 0).isRequired(false))
                        .queryParam(param -> param.key("calculation")
                                .value((calculation != null) ? calculation.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DataItem[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Data400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Data429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets the most recent data point for each specified device and data signal. The timestamps are
     * in UTC. _🔐 This endpoint requires the **Data** endpoint permission._ _This request can also
     * be made using the POST method, with a request to `realtimedata.json` and a JSON request body
     * instead of query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalIds  Required parameter: Which data signals to get data for.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  calculation  Optional parameter: The calculation used when aggregating data, both
     *         over time and across devices. The default is the data signal default.
     * @return    Returns the List of DataRealTimeItem wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DataRealTimeItem>> getRealTimeData(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationModeRealTime calculation) throws ApiException, IOException {
        return prepareGetRealTimeDataRequest(deviceIds, dataSignalIds, aggregate, aggregateLevel,
                calculation).execute();
    }

    /**
     * Gets the most recent data point for each specified device and data signal. The timestamps are
     * in UTC. _🔐 This endpoint requires the **Data** endpoint permission._ _This request can also
     * be made using the POST method, with a request to `realtimedata.json` and a JSON request body
     * instead of query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalIds  Required parameter: Which data signals to get data for.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  calculation  Optional parameter: The calculation used when aggregating data, both
     *         over time and across devices. The default is the data signal default.
     * @return    Returns the List of DataRealTimeItem wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DataRealTimeItem>>> getRealTimeDataAsync(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationModeRealTime calculation) {
        try {
            return prepareGetRealTimeDataRequest(deviceIds, dataSignalIds, aggregate, aggregateLevel,
            calculation).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getRealTimeData.
     */
    private ApiCall<ApiResponse<List<DataRealTimeItem>>, ApiException> prepareGetRealTimeDataRequest(
            final List<Integer> deviceIds,
            final List<Integer> dataSignalIds,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final CalculationModeRealTime calculation) {
        return new ApiCall.Builder<ApiResponse<List<DataRealTimeItem>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/realtimedata")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds))
                        .queryParam(param -> param.key("dataSignalIds")
                                .value(dataSignalIds))
                        .queryParam(param -> param.key("aggregate")
                                .value((aggregate != null) ? aggregate.value() : null).isRequired(false))
                        .queryParam(param -> param.key("aggregateLevel")
                                .value((aggregateLevel != null) ? aggregateLevel : 0).isRequired(false))
                        .queryParam(param -> param.key("calculation")
                                .value((calculation != null) ? calculation.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DataRealTimeItem[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Realtimedata400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Realtimedata429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets signal data aggregated per availability contract category. _🔐 This endpoint requires
     * the **Data** and **Statuses** endpoint permissions._ _This request can also be made using the
     * POST method, with a request to `datapercategory.json` and a JSON request body instead of
     * query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalId  Required parameter: Which signal to get data for; only Lost Production
     *         signals are supported at the moment.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  category  Optional parameter: Which status categories to include. By default all
     *         categories are included.
     * @param  contractType  Optional parameter: Which contract type to use if using multiple
     *         availability contracts.
     * @return    Returns the DatapercategoryResponse1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DatapercategoryResponse1> getDataPerCategory(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final List<StatusCategory> category,
            final ContractType1 contractType) throws ApiException, IOException {
        return prepareGetDataPerCategoryRequest(deviceIds, dataSignalId, timestampStart,
                timestampEnd, aggregate, aggregateLevel, category, contractType).execute();
    }

    /**
     * Gets signal data aggregated per availability contract category. _🔐 This endpoint requires
     * the **Data** and **Statuses** endpoint permissions._ _This request can also be made using the
     * POST method, with a request to `datapercategory.json` and a JSON request body instead of
     * query parameters._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalId  Required parameter: Which signal to get data for; only Lost Production
     *         signals are supported at the moment.
     * @param  timestampStart  Required parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Required parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @param  aggregate  Optional parameter: How the data should be aggregated with regards to
     *         device(s) or site(s).
     * @param  aggregateLevel  Optional parameter: When AggregateMode `siteLevel` is used this
     *         parameter controls down to which level in the hierarchy to aggregate.
     * @param  category  Optional parameter: Which status categories to include. By default all
     *         categories are included.
     * @param  contractType  Optional parameter: Which contract type to use if using multiple
     *         availability contracts.
     * @return    Returns the DatapercategoryResponse1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DatapercategoryResponse1>> getDataPerCategoryAsync(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final List<StatusCategory> category,
            final ContractType1 contractType) {
        try {
            return prepareGetDataPerCategoryRequest(deviceIds, dataSignalId, timestampStart, timestampEnd,
            aggregate, aggregateLevel, category, contractType).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getDataPerCategory.
     */
    private ApiCall<ApiResponse<DatapercategoryResponse1>, ApiException> prepareGetDataPerCategoryRequest(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd,
            final AggregateMode aggregate,
            final Integer aggregateLevel,
            final List<StatusCategory> category,
            final ContractType1 contractType) {
        return new ApiCall.Builder<ApiResponse<DatapercategoryResponse1>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/datapercategory")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds))
                        .queryParam(param -> param.key("dataSignalId")
                                .value(dataSignalId).isRequired(false))
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)))
                        .queryParam(param -> param.key("aggregate")
                                .value((aggregate != null) ? aggregate.value() : null).isRequired(false))
                        .queryParam(param -> param.key("aggregateLevel")
                                .value((aggregateLevel != null) ? aggregateLevel : 0).isRequired(false))
                        .queryParam(param -> param.key("category")
                                .value(StatusCategory.toValue(category)).isRequired(false))
                        .queryParam(param -> param.key("contractType")
                                .value((contractType != null) ? contractType.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DatapercategoryResponse1.class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Datapercategory400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Datapercategory429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * Gets high resolution data for a data signal for each specified device. The timestamps are in
     * UTC. The endpoint returns up to an hour's worth of high resolution data for the provided
     * device IDs and data signal ID. It is possible to request data for up to 10 separate devices
     * and one data signal ID. Timestamp start and end are optional. The default time span returned
     * is the latest hour. If supplied, timestamp start must be within the past 12 hours. Timestamp
     * end will by default be an hour after timestamp start but can be set for shorter intervals.
     * There is no high resolution data available for data signals that are calculated. The data for
     * those signals can be retrieved through the data endpoint. _🔐 This endpoint requires the
     * **HighResolution** endpoint permission._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalId  Required parameter: Which data signal to get data for.
     * @param  timestampStart  Optional parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Optional parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @return    Returns the List of HighresdataResponse wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<HighresdataResponse>> getHighResData(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd) throws ApiException, IOException {
        return prepareGetHighResDataRequest(deviceIds, dataSignalId, timestampStart,
                timestampEnd).execute();
    }

    /**
     * Gets high resolution data for a data signal for each specified device. The timestamps are in
     * UTC. The endpoint returns up to an hour's worth of high resolution data for the provided
     * device IDs and data signal ID. It is possible to request data for up to 10 separate devices
     * and one data signal ID. Timestamp start and end are optional. The default time span returned
     * is the latest hour. If supplied, timestamp start must be within the past 12 hours. Timestamp
     * end will by default be an hour after timestamp start but can be set for shorter intervals.
     * There is no high resolution data available for data signals that are calculated. The data for
     * those signals can be retrieved through the data endpoint. _🔐 This endpoint requires the
     * **HighResolution** endpoint permission._.
     * @param  deviceIds  Required parameter: Which devices to get data for.
     * @param  dataSignalId  Required parameter: Which data signal to get data for.
     * @param  timestampStart  Optional parameter: The beginning of the time interval to get data
     *         for (inclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         start timestamp **is** included in the time interval: for example, to select the full
     *         month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and `timestampEnd`
     *         to "2020-04-01T00:00:00". Timestamps selected in the portal will by default be in
     *         UTC.
     * @param  timestampEnd  Optional parameter: The end of the time interval to get data for
     *         (exclusive), in [RFC 3339, section
     *         5.6](https://tools.ietf.org/html/rfc3339#section-5.6) **date-time** format: *
     *         Timestamps ending with 'Z' are treated as UTC. Example: "2020-01-01T00:00:00Z" * Time
     *         zone (UTC) offset timestamps ending with '+HH:mm'/"-HH:mm" are also supported.
     *         Example: "2020-01-01T02:00:00-02:00" * Other timestamps are treated as being in the
     *         time zone configured in the Greenbyte Platform. Example: "2020-01-01T00:00:00" The
     *         end timestamp is **not** included in the time interval: for example, to select the
     *         full month of March 2020, set `timestampStart` to "2020-03-01T00:00:00" and
     *         `timestampEnd` to "2020-04-01T00:00:00". Timestamps selected in the portal will by
     *         default be in UTC.
     * @return    Returns the List of HighresdataResponse wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<HighresdataResponse>>> getHighResDataAsync(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd) {
        try {
            return prepareGetHighResDataRequest(deviceIds, dataSignalId, timestampStart,
            timestampEnd).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for getHighResData.
     */
    private ApiCall<ApiResponse<List<HighresdataResponse>>, ApiException> prepareGetHighResDataRequest(
            final List<Integer> deviceIds,
            final int dataSignalId,
            final LocalDateTime timestampStart,
            final LocalDateTime timestampEnd) {
        return new ApiCall.Builder<ApiResponse<List<HighresdataResponse>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/highresdata")
                        .queryParam(param -> param.key("deviceIds")
                                .value(deviceIds))
                        .queryParam(param -> param.key("dataSignalId")
                                .value(dataSignalId).isRequired(false))
                        .queryParam(param -> param.key("timestampStart")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampStart)).isRequired(false))
                        .queryParam(param -> param.key("timestampEnd")
                                .value(DateTimeHelper.toRfc8601DateTime(timestampEnd)).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .add("ApiKeyHeaderAuth"))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        HighresdataResponse[].class))
                        .nullify404(false)
                        .localErrorCase("400",
                                 ErrorCase.setReason("The request cannot be fulfilled due to bad syntax.",
                                (reason, context) -> new Highresdata400ErrorException(reason, context)))
                        .localErrorCase("401",
                                 ErrorCase.setReason("The request is missing a valid API key.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("403",
                                 ErrorCase.setReason("One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("405",
                                 ErrorCase.setReason("The HTTP method is not allowed for the endpoint.",
                                (reason, context) -> new ApiException(reason, context)))
                        .localErrorCase("429",
                                 ErrorCase.setReason("The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  \"1m\", \"12h\", or \"1d\").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.",
                                (reason, context) -> new Highresdata429ErrorException(reason, context)))
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}
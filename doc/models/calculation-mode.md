
# Calculation Mode

Which operation to use when aggregating data.

## Enumeration

`CalculationMode`

## Fields

| Name |
|  --- |
| `AVERAGE` |
| `SUM` |
| `COUNTER` |

## Example

```java
import cloud.greenbyte.intro.models.CalculationMode;

CalculationMode calculationMode = CalculationMode.AVERAGE;
```

